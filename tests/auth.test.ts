import { describe, it, expect } from 'vitest';

// اختبار أولي لنقطة تسجيل الدخول

describe('Auth API', () => {
  it('should return 401 for wrong credentials', async () => {
    // يمكنك استخدام supertest أو fetch لاختبار نقطة /api/auth/login
    expect(true).toBe(true);
  });
});
